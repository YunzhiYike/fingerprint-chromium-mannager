# 使用官方 Electron 镜像源，避免淘宝镜像的 TLS 证书问题
electron_mirror=https://github.com/electron/electron/releases/download/
electron_custom_dir=v37.2.3

# 确保使用官方 npm 源
registry=https://registry.npmjs.org/

# 设置缓存目录
cache=${HOME}/.npm

# 禁用进度条，减少日志输出
progress=false

# 设置日志级别
loglevel=warn
